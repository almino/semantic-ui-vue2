export default {
    props: {
        disabled: {
            type: Boolean,
            required: false,
            default: false,
            // validator(value) {
            //     return true
            // },
        },
    }
}
